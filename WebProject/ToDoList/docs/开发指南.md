# ToDoList 开发指南

## 概述

本文档为 ToDoList 项目的开发指南，旨在帮助开发者快速上手项目开发，包括开发环境搭建、代码规范、开发流程、测试指南、调试技巧等内容。

## 开发环境搭建

### 前置要求

- **Node.js**: 22.0.0 或更高版本
- **pnpm**: 8.0.0 或更高版本
- **Docker**: 20.10.0 或更高版本（可选）
- **Docker Compose**: 2.0.0 或更高版本（可选）
- **Git**: 2.30.0 或更高版本
- **VS Code**（推荐）或其他代码编辑器

### 安装步骤

#### 1. 克隆项目

```bash
git clone <repository-url>
cd ToDoList
```

#### 2. 安装 Node.js

```bash
# 使用 nvm 安装 Node.js（推荐）
nvm install 22
nvm use 22

# 验证安装
node --version
npm --version
```

#### 3. 安装 pnpm

```bash
# 使用 npm 安装 pnpm
npm install -g pnpm

# 启用 corepack（推荐）
corepack enable

# 验证安装
pnpm --version
```

#### 4. 安装依赖

```bash
# 进入服务端目录
cd server

# 安装依赖
pnpm install
```

#### 5. 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env.development

# 编辑环境变量文件
vim .env.development
```

#### 6. 启动数据库

**方式一：使用 Docker（推荐）**

```bash
# 启动 MySQL 容器
docker-compose -f docker-compose.dev.yml up -d mysql

# 查看容器状态
docker-compose ps
```

**方式二：本地安装 MySQL**

```bash
# macOS
brew install mysql
brew services start mysql

# Ubuntu
sudo apt-get install mysql-server
sudo systemctl start mysql
```

#### 7. 初始化数据库

```bash
# 运行数据库迁移
pnpm migration:run

# 或者使用 TypeORM 同步（仅开发环境）
# 在 .env.development 中设置 DB_SYNCHRONIZE=true
```

#### 8. 启动开发服务器

```bash
# 启动开发服务器（热重载）
pnpm run start:dev

# 或者使用 Docker
docker-compose -f docker-compose.dev.yml up -d
```

#### 9. 验证安装

```bash
# 访问健康检查端点
curl http://localhost:3000

# 访问 API 文档
open http://localhost:3000/api
```

## 项目结构

### 目录结构

```
server/
├── src/                          # 源代码目录
│   ├── common/                   # 公共模块
│   │   ├── constants/            # 常量定义
│   │   │   ├── index.ts
│   │   │   └── roles.constant.ts
│   │   ├── decorators/           # 装饰器
│   │   │   ├── index.ts
│   │   │   ├── public.decorator.ts
│   │   │   ├── roles.decorator.ts
│   │   │   ├── throttle.decorator.ts
│   │   │   └── user.decorator.ts
│   │   ├── filters/              # 异常过滤器
│   │   │   ├── index.ts
│   │   │   └── http-exception.filter.ts
│   │   ├── guards/               # 守卫
│   │   │   ├── index.ts
│   │   │   ├── auth.guard.ts
│   │   │   ├── roles.guard.ts
│   │   │   └── throttler.guard.ts
│   │   ├── interceptors/         # 拦截器
│   │   │   ├── index.ts
│   │   │   ├── logging.interceptor.ts
│   │   │   ├── timeout.interceptor.ts
│   │   │   └── transform.interceptor.ts
│   │   ├── interfaces/           # 接口定义
│   │   │   └── index.ts
│   │   ├── pipes/                # 管道
│   │   │   ├── index.ts
│   │   │   ├── parse-int.pipe.ts
│   │   │   └── validation.pipe.ts
│   │   ├── utils/                # 工具函数
│   │   │   ├── index.ts
│   │   │   ├── date.util.ts
│   │   │   ├── pagination.util.ts
│   │   │   ├── response.util.ts
│   │   │   └── string.util.ts
│   │   ├── common.module.ts      # 公共模块定义
│   │   └── index.ts              # 导出所有公共模块
│   ├── config/                   # 配置文件
│   │   ├── validation.config.ts  # 验证配置
│   │   └── winston.config.ts     # 日志配置
│   ├── database/                 # 数据库相关
│   │   ├── repositories/         # 仓储
│   │   │   ├── base.repository.ts
│   │   │   └── index.ts
│   │   ├── seeds/                # 数据种子
│   │   │   ├── base.seed.ts
│   │   │   ├── user.seed.ts
│   │   │   └── index.ts
│   │   ├── database.module.ts    # 数据库模块
│   │   └── index.ts
│   ├── dto/                      # 数据传输对象
│   │   ├── index.ts
│   │   ├── todo.dto.ts
│   │   └── user.dto.ts
│   ├── entities/                 # 实体定义
│   │   ├── index.ts
│   │   ├── todo.entity.ts
│   │   └── user.entity.ts
│   ├── modules/                  # 业务模块
│   │   ├── todos/                # 待办事项模块
│   │   │   ├── dto/
│   │   │   │   ├── create-todo.dto.ts
│   │   │   │   ├── index.ts
│   │   │   │   └── update-todo.dto.ts
│   │   │   ├── todos.controller.ts
│   │   │   ├── todos.module.ts
│   │   │   └── todos.service.ts
│   │   └── users/                # 用户模块
│   │       ├── dto/
│   │       │   ├── create-user.dto.ts
│   │       │   ├── index.ts
│   │       │   └── update-user.dto.ts
│   │       ├── users.controller.ts
│   │       ├── users.module.ts
│   │       └── users.service.ts
│   ├── types/                    # 类型定义
│   │   └── index.ts
│   ├── app.module.ts             # 应用模块
│   └── main.ts                   # 应用入口
├── test/                         # 测试文件
│   ├── app.e2e-spec.ts           # E2E 测试
│   └── jest-e2e.json             # Jest E2E 配置
├── .env.development              # 开发环境变量
├── .env.example                  # 环境变量示例
├── .env.production               # 生产环境变量
├── .env.test                     # 测试环境变量
├── .gitignore                    # Git 忽略文件
├── .prettierrc                   # Prettier 配置
├── Dockerfile                    # Docker 镜像构建文件
├── docker-compose.dev.yml        # Docker Compose 开发环境
├── docker-compose.yml            # Docker Compose 生产环境
├── eslint.config.mjs             # ESLint 配置
├── nest-cli.json                 # NestJS CLI 配置
├── package.json                  # 项目依赖
├── pnpm-lock.yaml                # pnpm 锁文件
├── tsconfig.build.json           # TypeScript 构建配置
├── tsconfig.json                 # TypeScript 配置
└── README.md                     # 项目说明
```

### 核心文件说明

| 文件 | 说明 |
|------|------|
| [main.ts](server/src/main.ts) | 应用入口，配置全局中间件、管道、拦截器等 |
| [app.module.ts](server/src/app.module.ts) | 根模块，导入所有功能模块 |
| [common/](server/src/common/) | 公共模块，包含过滤器、拦截器、守卫等 |
| [entities/](server/src/entities/) | TypeORM 实体定义 |
| [modules/](server/src/modules/) | 业务模块，每个模块包含 controller、service、dto |

## 代码规范

### TypeScript 规范

#### 1. 命名规范

```typescript
// 类名：PascalCase
class UserService {}
class TodoController {}

// 接口：PascalCase，以 I 开头（可选）
interface IUserRepository {}

// 变量和函数：camelCase
const userName = 'John';
function getUserById() {}

// 常量：UPPER_SNAKE_CASE
const MAX_RETRY_COUNT = 3;
const API_BASE_URL = 'https://api.example.com';

// 私有属性：以 _ 开头
class UserService {
  private _userRepository: UserRepository;
}

// 枚举：PascalCase
enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  GUEST = 'guest',
}
```

#### 2. 类型定义

```typescript
// 使用接口定义对象类型
interface CreateUserDto {
  username: string;
  email: string;
  password: string;
}

// 使用类型别名定义联合类型
type UserRole = 'admin' | 'user' | 'guest';

// 使用泛型提高代码复用性
interface ApiResponse<T> {
  success: boolean;
  data: T;
  message: string;
}

// 使用枚举定义固定值
enum TodoStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
}
```

#### 3. 异步处理

```typescript
// 使用 async/await 而不是 Promise.then()
async function getUserById(id: number): Promise<User> {
  const user = await this.userRepository.findOne({ where: { id } });
  return user;
}

// 错误处理
async function getUserById(id: number): Promise<User> {
  try {
    const user = await this.userRepository.findOne({ where: { id } });
    if (!user) {
      throw new NotFoundException('User not found');
    }
    return user;
  } catch (error) {
    throw new InternalServerErrorException('Failed to fetch user');
  }
}
```

### NestJS 规范

#### 1. Controller 规范

```typescript
import { Controller, Get, Post, Body, Param, HttpCode, HttpStatus } from '@nestjs/common';
import { ApiTags, ApiOperation } from '@nestjs/swagger';

@ApiTags('users')
@Controller('users')
export class UsersController {
  constructor(private readonly usersService: UsersService) {}

  @Get()
  @ApiOperation({ summary: 'Get all users' })
  findAll() {
    return this.usersService.findAll();
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get user by ID' })
  findOne(@Param('id') id: string) {
    return this.usersService.findOne(+id);
  }

  @Post()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({ summary: 'Create a new user' })
  create(@Body() createUserDto: CreateUserDto) {
    return this.usersService.create(createUserDto);
  }
}
```

#### 2. Service 规范

```typescript
import { Injectable, NotFoundException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { User } from '../entities/user.entity';
import { CreateUserDto } from './dto/create-user.dto';

@Injectable()
export class UsersService {
  constructor(
    @InjectRepository(User)
    private readonly userRepository: Repository<User>,
  ) {}

  async create(createUserDto: CreateUserDto): Promise<User> {
    const user = this.userRepository.create(createUserDto);
    return this.userRepository.save(user);
  }

  async findAll(): Promise<User[]> {
    return this.userRepository.find();
  }

  async findOne(id: number): Promise<User> {
    const user = await this.userRepository.findOne({ where: { id } });
    if (!user) {
      throw new NotFoundException('User not found');
    }
    return user;
  }
}
```

#### 3. DTO 规范

```typescript
import { IsString, IsEmail, MinLength, MaxLength } from 'class-validator';

export class CreateUserDto {
  @IsString()
  @MinLength(3)
  @MaxLength(20)
  username!: string;

  @IsEmail()
  email!: string;

  @IsString()
  @MinLength(6)
  password!: string;
}

export class UpdateUserDto {
  @IsString()
  @MinLength(3)
  @MaxLength(20)
  username?: string;

  @IsEmail()
  email?: string;

  @IsString()
  @MinLength(6)
  password?: string;
}
```

### Git 提交规范

#### 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### Type 类型

- **feat**: 新功能
- **fix**: 修复 bug
- **docs**: 文档更新
- **style**: 代码格式调整（不影响代码运行）
- **refactor**: 重构（既不是新增功能，也不是修复 bug）
- **perf**: 性能优化
- **test**: 测试相关
- **chore**: 构建过程或辅助工具的变动

#### 示例

```bash
# 新功能
git commit -m "feat(users): add user registration endpoint"

# 修复 bug
git commit -m "fix(todos): resolve issue with todo status update"

# 文档更新
git commit -m "docs(readme): update installation instructions"

# 重构
git commit -m "refactor(auth): improve JWT token validation logic"
```

## 开发流程

### 功能开发流程

#### 1. 创建功能分支

```bash
# 从 main 分支创建功能分支
git checkout main
git pull origin main
git checkout -b feature/user-profile

# 或者使用约定式提交
git checkout -b feat/add-user-profile
```

#### 2. 开发功能

```bash
# 启动开发服务器
pnpm run start:dev

# 修改代码
# ...

# 运行测试
pnpm run test
```

#### 3. 提交代码

```bash
# 添加修改的文件
git add .

# 提交代码
git commit -m "feat(users): add user profile endpoint"

# 推送到远程仓库
git push origin feature/user-profile
```

#### 4. 创建 Pull Request

1. 在 GitHub/GitLab 上创建 Pull Request
2. 填写 PR 描述，包括：
   - 功能说明
   - 测试情况
   - 相关 issue
3. 请求代码审查
4. 根据审查意见修改代码
5. 合并到主分支

#### 5. 清理分支

```bash
# 切换到主分支
git checkout main
git pull origin main

# 删除已合并的分支
git branch -d feature/user-profile
git push origin --delete feature/user-profile
```

### Bug 修复流程

#### 1. 创建修复分支

```bash
# 从 main 分支创建修复分支
git checkout main
git pull origin main
git checkout -b fix/todo-status-update

# 或者使用约定式提交
git checkout -b fix/resolve-todo-status-update-issue
```

#### 2. 定位和修复问题

```bash
# 查看日志
pnpm run start:dev

# 运行测试
pnpm run test

# 修复代码
# ...
```

#### 3. 验证修复

```bash
# 运行相关测试
pnpm run test -- todo.service.spec

# 运行所有测试
pnpm run test

# 手动测试修复的功能
```

#### 4. 提交和合并

```bash
# 提交修复
git add .
git commit -m "fix(todos): resolve issue with todo status update"
git push origin fix/todo-status-update

# 创建 PR 并合并
# ...
```

## 测试指南

### 测试类型

#### 1. 单元测试

测试单个函数、类或组件的功能。

**示例**:

```typescript
import { Test, TestingModule } from '@nestjs/testing';
import { UsersService } from './users.service';
import { getRepositoryToken } from '@nestjs/typeorm';
import { User } from '../entities/user.entity';

describe('UsersService', () => {
  let service: UsersService;
  let mockUserRepository: any;

  beforeEach(async () => {
    mockUserRepository = {
      create: jest.fn(),
      save: jest.fn(),
      findOne: jest.fn(),
      find: jest.fn(),
      update: jest.fn(),
      delete: jest.fn(),
    };

    const module: TestingModule = await Test.createTestingModule({
      providers: [
        UsersService,
        {
          provide: getRepositoryToken(User),
          useValue: mockUserRepository,
        },
      ],
    }).compile();

    service = module.get<UsersService>(UsersService);
  });

  it('should be defined', () => {
    expect(service).toBeDefined();
  });

  describe('create', () => {
    it('should create a new user', async () => {
      const createUserDto = {
        username: 'testuser',
        email: 'test@example.com',
        password: 'password123',
      };

      const expectedUser = {
        id: 1,
        ...createUserDto,
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      mockUserRepository.create.mockReturnValue(expectedUser);
      mockUserRepository.save.mockResolvedValue(expectedUser);

      const result = await service.create(createUserDto);

      expect(mockUserRepository.create).toHaveBeenCalledWith(createUserDto);
      expect(mockUserRepository.save).toHaveBeenCalled();
      expect(result).toEqual(expectedUser);
    });
  });
});
```

#### 2. 集成测试

测试多个模块或组件之间的交互。

**示例**:

```typescript
import { Test, TestingModule } from '@nestjs/testing';
import { INestApplication, ValidationPipe } from '@nestjs/common';
import * as request from 'supertest';
import { AppModule } from '../src/app.module';

describe('UsersController (e2e)', () => {
  let app: INestApplication;

  beforeEach(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleFixture.createNestApplication();
    app.useGlobalPipes(new ValidationPipe());
    await app.init();
  });

  afterAll(async () => {
    await app.close();
  });

  describe('/users (POST)', () => {
    it('should create a new user', () => {
      return request(app.getHttpServer())
        .post('/users')
        .send({
          username: 'testuser',
          email: 'test@example.com',
          password: 'password123',
        })
        .expect(201)
        .expect((res) => {
          expect(res.body).toHaveProperty('id');
          expect(res.body.username).toBe('testuser');
          expect(res.body.email).toBe('test@example.com');
          expect(res.body).not.toHaveProperty('password');
        });
    });

    it('should return 400 for invalid data', () => {
      return request(app.getHttpServer())
        .post('/users')
        .send({
          username: 'ab',
          email: 'invalid-email',
          password: '123',
        })
        .expect(400);
    });
  });
});
```

#### 3. E2E 测试

测试整个应用程序的端到端流程。

**示例**:

```typescript
import { Test, TestingModule } from '@nestjs/testing';
import { INestApplication } from '@nestjs/common';
import * as request from 'supertest';
import { AppModule } from './../src/app.module';

describe('AppController (e2e)', () => {
  let app: INestApplication;

  beforeEach(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleFixture.createNestApplication();
    await app.init();
  });

  it('/ (GET)', () => {
    return request(app.getHttpServer())
      .get('/')
      .expect(200)
      .expect('Hello World!');
  });
});
```

### 运行测试

```bash
# 运行所有测试
pnpm run test

# 运行单元测试
pnpm run test:unit

# 运行 E2E 测试
pnpm run test:e2e

# 运行测试并生成覆盖率报告
pnpm run test:cov

# 运行特定测试文件
pnpm run test users.service.spec

# 监听模式运行测试
pnpm run test:watch
```

### 测试覆盖率

目标覆盖率：
- **语句覆盖率**: ≥ 80%
- **分支覆盖率**: ≥ 75%
- **函数覆盖率**: ≥ 80%
- **行覆盖率**: ≥ 80%

## 调试技巧

### VS Code 调试配置

创建 `.vscode/launch.json` 文件：

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug NestJS",
      "runtimeExecutable": "pnpm",
      "runtimeArgs": ["run", "start:debug"],
      "console": "integratedTerminal",
      "restart": true,
      "protocol": "inspector",
      "env": {
        "NODE_ENV": "development"
      }
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Jest Tests",
      "runtimeExecutable": "pnpm",
      "runtimeArgs": ["run", "test:debug", "--", "--runInBand"],
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen"
    }
  ]
}
```

### 日志调试

使用 Winston 日志记录调试信息：

```typescript
import { Logger } from '@nestjs/common';

@Injectable()
export class UsersService {
  private readonly logger = new Logger(UsersService.name);

  async findOne(id: number): Promise<User> {
    this.logger.debug(`Finding user with ID: ${id}`);
    
    const user = await this.userRepository.findOne({ where: { id } });
    
    if (!user) {
      this.logger.warn(`User not found with ID: ${id}`);
      throw new NotFoundException('User not found');
    }
    
    this.logger.debug(`User found: ${user.username}`);
    return user;
  }
}
```

### 断点调试

在代码中设置断点：

```typescript
async findOne(id: number): Promise<User> {
  debugger; // 设置断点
  
  const user = await this.userRepository.findOne({ where: { id } });
  
  if (!user) {
    throw new NotFoundException('User not found');
  }
  
  return user;
}
```

### 常用调试命令

```bash
# 启动调试模式
pnpm run start:debug

# 查看日志
tail -f logs/app-$(date +%Y-%m-%d).log

# 查看错误日志
tail -f logs/error-$(date +%Y-%m-%d).log

# 使用 Docker 查看日志
docker-compose logs -f app
```

## 常见问题

### 1. 端口被占用

**问题**: 启动应用时提示端口 3000 被占用。

**解决方案**:

```bash
# 查找占用端口的进程
lsof -i :3000

# 杀死进程
kill -9 <PID>

# 或者修改端口
# 在 .env.development 中设置 PORT=3001
```

### 2. 数据库连接失败

**问题**: 无法连接到数据库。

**解决方案**:

```bash
# 检查数据库是否运行
docker-compose ps mysql

# 查看数据库日志
docker-compose logs mysql

# 检查环境变量配置
cat .env.development

# 重启数据库
docker-compose restart mysql
```

### 3. 依赖安装失败

**问题**: pnpm install 失败。

**解决方案**:

```bash
# 清理缓存
pnpm store prune

# 删除 node_modules 和锁文件
rm -rf node_modules pnpm-lock.yaml

# 重新安装
pnpm install

# 或者使用 npm
npm install
```

### 4. TypeScript 编译错误

**问题**: TypeScript 编译报错。

**解决方案**:

```bash
# 检查 TypeScript 版本
pnpm list typescript

# 重新构建
pnpm run build

# 检查类型错误
pnpm run typecheck
```

### 5. 测试失败

**问题**: 测试运行失败。

**解决方案**:

```bash
# 查看详细错误信息
pnpm run test -- --verbose

# 运行特定测试
pnpm run test -- users.service.spec

# 清理测试缓存
pnpm run test:clear

# 重新运行测试
pnpm run test
```

## 最佳实践

### 1. 代码组织

- 按功能模块组织代码
- 保持单一职责原则
- 避免循环依赖
- 使用依赖注入

### 2. 错误处理

```typescript
// 使用 NestJS 内置异常
throw new NotFoundException('User not found');
throw new BadRequestException('Invalid input');
throw new UnauthorizedException('Unauthorized');

// 自定义异常
@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  catch(exception: unknown, host: ArgumentsHost) {
    // 处理所有异常
  }
}
```

### 3. 性能优化

```typescript
// 使用缓存
@Cacheable(60) // 缓存 60 秒
async findAll(): Promise<User[]> {
  return this.userRepository.find();
}

// 使用分页
async findAll(page: number, limit: number): Promise<User[]> {
  return this.userRepository.find({
    skip: (page - 1) * limit,
    take: limit,
  });
}

// 使用连接池
{
  type: 'mysql',
  extra: {
    connectionLimit: 20,
  },
}
```

### 4. 安全实践

```typescript
// 输入验证
export class CreateUserDto {
  @IsString()
  @MinLength(3)
  @MaxLength(20)
  username!: string;
}

// 密码加密
import * as bcrypt from 'bcrypt';

async hashPassword(password: string): Promise<string> {
  return bcrypt.hash(password, 10);
}

// SQL 注入防护
// 使用 TypeORM 参数化查询
const user = await this.userRepository.findOne({ where: { id } });
```

### 5. 日志记录

```typescript
// 使用不同日志级别
this.logger.debug('Debug message');
this.logger.log('Info message');
this.logger.warn('Warning message');
this.logger.error('Error message');

// 记录上下文信息
this.logger.log(`User ${userId} performed action ${action}`);

// 记录异常
this.logger.error('Failed to create user', error.stack);
```

## 资源链接

### 官方文档

- [NestJS 官方文档](https://docs.nestjs.com/)
- [TypeScript 官方文档](https://www.typescriptlang.org/docs/)
- [TypeORM 官方文档](https://typeorm.io/)
- [Jest 官方文档](https://jestjs.io/docs/getting-started)

### 社区资源

- [NestJS GitHub](https://github.com/nestjs/nest)
- [NestJS Discord](https://discord.gg/G7Qnnhy)
- [Stack Overflow - NestJS](https://stackoverflow.com/questions/tagged/nestjs)

### 工具推荐

- [Postman](https://www.postman.com/) - API 测试工具
- [Insomnia](https://insomnia.rest/) - REST 客户端
- [DBeaver](https://dbeaver.io/) - 数据库管理工具
- [TablePlus](https://tableplus.com/) - 数据库 GUI 工具

---

**最后更新**: 2024-01-01
